- name: vars promt
  hosts: local
  connection: local
  vars_promt:
    - name: trainer
      promt: "enter trainer"
      private: false
  tasks:
  - name: vars promts
    vars:
      course: devops
    ansible.builtin.debug:
      msg: "trainer: {{ trainer }}, course: {{ course }}, duration: {{ password }}"