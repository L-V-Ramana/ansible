- name: loops on installitions
  hosts: frontend
  become: yes
  tasks:
  - name: installitions
    ansible.builtin.package:
      name: "{{ item }}"
      state: present
    loop:
    - nginx
    - shell
    - mongodb