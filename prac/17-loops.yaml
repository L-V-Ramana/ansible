- name: loops
  hosts: frontend
  tasks:
  - name: printing from map
    ansible.builtin.package:
      name: "{{item.name}}"
      state: "{{item.state}}"
    loop:
    - {name: 'nginx',state: 'present'}
    - {name: 'mysql', state: 'installed'}
    - {name: 'zip',state: 'absent'}