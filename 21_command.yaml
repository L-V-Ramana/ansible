- name: shell vs command
  hosts: local
  connection: local
  tasks:
  - name: redirecting
    ansible.builtin.command: "ls -ltr"
    register: output
  
  - name: printing output
    ansible.builtin.debug:
      msg: "{{ output }}" 