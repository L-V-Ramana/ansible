- name: passing arguments from command 
  hosts: local 
  connection: local 
  tasks:
  - name: collecting arguments from arguments
    ansible.buitin.debug:
       msg: "information recived from the arguments as {{ course }} and {{ batch }}"

